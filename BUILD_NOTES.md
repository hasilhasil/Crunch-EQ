Crunch EQ 备份说明（260813）
================================

本备份包含三部分：
1. Crunch EQ Source/  —— 插件源代码与工程文件（CMake 构建）
2. Crunch EQ.vst3/    —— 已编译好的 VST3 插件本体（可直接安装）
3. JUCE.tar.gz        —— 构建依赖 JUCE 8.0.4 源码包（用于离线重编译）

------------------------------------------------
【一、直接安装插件（不改代码时）】
把 "Crunch EQ.vst3" 整个文件夹复制到：
    C:\Program Files\Common Files\VST3\
然后在 DAW（如 FL Studio）里 Options -> Manage plugins -> Rescan 即可加载 "Crunch EQ"。

------------------------------------------------
【二、重新编译 / 修改代码】
1. 解压 JUCE.tar.gz，得到 JUCE-8.0.4 目录。
2. 用 CMake 配置工程（把下面的 <路径> 换成实际绝对路径）：
     cmake -B build -A x64 -DJUCE_DIR="<JUCE-8.0.4 目录>" "Crunch EQ Source"
3. 编译：
     cmake --build build --config Release --target ProQ3Clone_VST3
4. 产物位于：
     build\ProQ3Clone_artefacts\Release\VST3\Crunch EQ.vst3
5. 安装：把该 .vst3 整个文件夹复制到 C:\Program Files\Common Files\VST3\，
   覆盖同名文件夹即可。

（如果不想用本地 JUCE，也可以不传 -DJUCE_DIR，让 CMakeLists.txt
  通过 FetchContent 自动从 GitHub 下载 JUCE 8.0.4，但需要联网。）

-----------------------------------------------
【二之二、UI 与 Crunch Compressor 对齐（260920）】
底部控制区已按 Crunch Compressor 的下半部分（BandControlPanel +
CrunchLookAndFeel）重做，两个插件现在共用同一套视觉语言：
  · CrunchPalette 调色板 + CrunchLookAndFeel（奶油 Cream 主题 / 珊瑚强调色）
  · 旋钮：暖灰环形轨道 + 强调色圆弧 + 近白色旋钮帽（带投影）+ 深色指针
  · 下拉框：圆角浅色底 + 强调色箭头；按钮：圆角药丸，开启时填充强调色
  · 内嵌 Poppins 字体（src\ui\Fonts\，由 CMake 的 juce_add_binary_data 打包）
  · 布局：左侧 COLOUR 模块 + 带标题的选择器网格；右侧主旋钮行
    （Freq/Gain/Q，强调色标题）+ TYPE/SLOPE 选择器 + Bypass 药丸；
    底部 In/Out 旋钮条
  · 主题菜单第三项改为 "Theme: Cream"（即压缩机的奶油主题）
注意：src\ui\Fonts\ 下的 Poppins 字体是编译必需资源，删除会导致构建失败。

-----------------------------------------------
【二之三、频谱/曲线动画性能修复（260920）】
症状：频谱与曲线动画卡顿、掉帧（频段越多越明显）。
原因与修复（只改 UI 与显示用辅助代码，DSP 音质不变）：
  1. 响应曲线原先每帧重算 320 点 × 24 频段：每点每段都重新设计双二阶系数
     （堆分配 + 三角函数 + 复数除法），8 段生效约 4 ms/帧，24 段时超过
     16.6 ms 直接掉帧。现在参数变化时才重算（responseDirty_），系数每段只
     设计一次，cos/sin 每点只算一次，并用 |H|² 求和取 dB（与原来乘积取 dB 等价）。
     实测曲线耗时 3.9~4.4 ms → 0.06 ms。
  2. 网格/坐标刻度/背景（含文字排版）缓存为 Image，每帧只 blit，
     尺寸/主题/dB 刻度变化时才重建。
  3. SpectrumAnalyzer 的对数频率查表只建一次；新增 FFT 帧计数器与帧变化量，
     画面静止时完全不重绘（空闲时 UI 线程 0 占用）。
  4. 分析缓冲在 UI 侧循环复用（swap 归还给处理器），音频线程不再逐块分配内存。
实测（Standalone，8 段全类型生效）：单帧绘制 4.18 ms → 0.18 ms；
最坏情况（每帧都重算曲线）0.29 ms，稳定 60 fps。

-----------------------------------------------
【二之四、频谱动画流畅度修复（260920 第二次）】
症状：频谱动画仍不流畅（像掉帧），旁边 IN/OUT 电平表却很顺。
原因：这两处的数据更新率不同——
  · 电平表：每个 UI tick 都刷新一次，60 fps。
  · 频谱：靠 FFT 的 FIFO 攒满一整个窗（2048 点）才出一帧，宿主每个 tick
    只送来约 800 个采样 → 实测只有 46.8 fps 频谱帧，且 0.78 帧/tick 导致
    “有时这一帧和上一帧完全相同”，看起来就是一顿一顿的。
修复：
  1. 帧率改为按“时间”驱动：每 ~11 ms（90 Hz）必出一帧，窗口仍是完整 2048 点，
     相邻帧重叠 ~75%（常规分析器做法）。实测 46.8 → 90.1 fps，即每一帧重绘
     都有新数据，与电平表同节奏。
  2. 频谱显示改为“对数频带能量平均”：每个显示点取一个 log 像素宽的 FFT 区间
     做能量(RMS)平均，而不是在两个 bin 之间插值，消除逐 bin 抖动导致的
     “沸腾”感。实测频谱值仍正常（mean≈-40 dB，无 NaN/爆值）。
  3. 分析缓冲改为每 tick 只推本 tick 的新采样（避免上 tick 数据被重复分析），
     同时保留容量复用，音频线程不额外分配。
实测：UI tick 59.4/s、重绘 = tick、频谱数据 90.0/s、paint 0.22 ms、
帧间隔 <20 ms 占 95%（稳定 60 fps）。

-----------------------------------------------
【二之五、低频频谱被量化成方块的修复（260920 第三次）】
症状：上一版把频谱显示改成“对数频带能量平均”后，低频率段变成方块状，
不再圆滑。
原因：低频率下单个 FFT bin（23 Hz @48kHz）比一个显示像素宽得多
（10 Hz 处一个 bin 横跨约 9 个像素），此时“取该 band 内所有 bin 的平均”
退化成“每个像素重复同一个 bin 的值”→ 明显台阶/方块。
修复（两段式重采样，取两者之长）：
  · 高频（一个像素跨多个 bin）：能量(RMS)平均覆盖的 bin，抑制逐 bin 抖动；
  · 低频（一个 bin 跨多个像素）：恢复原来的“相邻两 bin 线性插值”，
    保持圆滑曲线。
判定条件：该像素对应的频率带宽度 < 1 个 bin 时用插值，否则用平均。
实测 A/B（同一扫频信号，统计 512 个显示点中“与上一点数值相同”的比例）：
  · 纯 band 平均（出问题的版本）：0.462~0.474（约一半相邻点相同 → 方块）
  · 修复后：0.012~0.067（圆滑）
90 Hz 帧间隔、60 fps 重绘等其它指标不变。

-----------------------------------------------
【二之六、宿主持续卡顿的根治：A(固定帧率分析) + B(显示层平滑)（260920）】
症状：单机版 Spectrum 流畅，但 FL Studio 里加载后卡顿（IN/OUT 表正常），
FabFilter Pro-Q3 在 FL 里不卡。
原因：宿主以自己决定的块大小给插件送音频（FL mixer 常见 1024-4096 采样）。
原来 FFT 由 UI tick(60Hz) 拉取，频谱数据更新率实际 = 48000 ÷ 宿主块大小
（4096 → 12 fps），FFT 帧还成簇到达；而电平表每 tick 都有新峰值 → 表顺、谱卡。
A：把 FFT 从 UI tick 上摘下来
  · 音频线程只写无锁环形缓冲（SpectrumEngine，无锁无分配，永不被阻塞）；
  · 后台线程每 ~5ms 醒一次，把环形缓冲喂给分析器；
  · 关键是“按显示节奏发射”：每 16ms 只产出一帧并发布（三槽无锁双缓冲）。
    之前按采样数 hop=90Hz 产帧时，一个 4096 块的音频会一次性产出 8 帧，
    UI 只读到最新一帧，其余被丢 → 仍按块率更新；改成按时间节流后才对齐。
    窗长仍 2048， hop = 采样率/60，相邻窗 61% 重叠，时间上严格跟真实走。
B：显示层做攻击/释放平滑 + 峰值保持（对齐 Pro-Q 观感）
  · UI 每 tick 把显示曲线按时间常数（起 30ms / 放 120ms）趋近最新帧，
    时间基准独立于帧率；峰值保持线每 tick 衰减 12 dB/s；
  · 曲线仍在趋近时继续重绘（spectrumMoving_），保证运动连续不停顿；
  · 空闲/收敛时不重绘（UI 线程零占用）。
实测（扫频信号 + 按块模拟宿主 1024/2048/4096/8192 采样，截取绘图区统计
“相邻两帧是否相同”）：
  · 修复前（不做 B，仅按块更新）：25 ms 采样下 17/39 对冻结 → ~23 fps
  · 修复后：1024/2048/4096/8192 全部 0/39 对冻结 → 全程 60 fps 无冻结
  · 内部：发布帧 59.6~71.6/s（= 每 tick 一帧）、paint = 60/s
  · idle 时不重绘；CPU 2.05s / 8s（含 JUCE Standalone 自身开销）

-----------------------------------------------
【二之七、频谱图形样式退回上一版（保留 A 的修复，去掉 B 的观感改动）（260920）】
需求：不改插件 UI，把频谱图形样式改回上一版，但不能带回上一版的 bug。
上一版（A+B 之前）的频谱样式：
  · Pre：强调色渐变填充曲线下方；Post：curveBase 色 2px 细线；无峰值保持线
  · 曲线值直接取分析器输出（分析器内部按帧 0.5 系数做时间平滑）
本次改动（只动绘制/取值，不动 UI 与 DSP）：
  · 删除 B 的显示层攻击/释放平滑：UI 直接把引擎发布的点用于绘制
  · 删除 B 的峰值保持线绘制与其状态数组
  · 重绘条件回到“本 tick 有新帧 或 电平表在动”
保留（所以上一版的 bug 不会回来）：
  · A：无锁环形缓冲 + 后台线程 + 每 16ms 只发布一帧（与显示节拍一致）
  · 低频频谱插值/高频能量平均的两段式重采样（低频不会变方块）
实测（模拟宿主块 1024/2048/4096/8192，只截绘图区统计相邻帧是否相同）：
  · 频谱可见更新：四种块大小全部 0/39 对冻结（全程流畅，无上一版卡顿）
  · 低频平滑度 repeatRatio 0.020~0.088（方块 bug 版本为 0.46~0.47）

-----------------------------------------------
【二之八、频谱三层叠加（灰=干音 / 蓝=输出 / 白描边）（260920）】
绘制顺序（自下而上）：
  1. 灰层：ORIGINAL DRY 信号（processBlock 最开头对输入 buffer 做的单声道和，
     EQ 与染色之前）→ CrunchPalette::inputFill，渐变填充 + 1px 描边
  2. 蓝层：FINAL OUTPUT（EQ + 染色 + 输出增益之后的单声道和）→ 主题强调色
     渐变填充（原来的样式）
  3. 白描边：蓝层顶边的白色曲线（Colours::white，95% 不透明度，2px）
视觉效果：EQ 衰减处灰层露出在蓝层之外，增益处蓝层超出灰层。
Analyzer 模式语义不变：Pre=只画灰层，Post=只画蓝层+白描边，Pre+Post=三层。
顺带修掉一个真 bug：引擎里
    (wantPre && pre.processPendingFrame()) || (wantPost && post.processPendingFrame())
  因 || 短路，只要 pre 产出帧 post 就永远不会被推进（postFrames 恒为 0、
  内部 FIFO 撑到上限），导致“Post/Pre+Post 模式的后级曲线根本没有数据”。
  改为两个布尔量分别求值。
实测（测试构建：6 个频段带 ±6/+9 dB，扫频信号）：
  pre/post 两路 RMS 明显不同（0.264→0.375 等）；
  绘图区像素统计：灰 8089 / 蓝 59975 / 白描边 14477；
  逐列扫描可见灰层在下、蓝层在上、白描边压在蓝层顶边。
注：测试时若 Standalone 里参数不生效，是 JUCE Standalone 会把上次的插件状态
持久化到 %APPDATA%\Crunch EQ\Crunch EQ.settings，删掉该文件即可恢复默认值。

-----------------------------------------------
【二之九、频谱反应延迟修复（260920）】
症状：播放音频时频谱反应有延迟（实测约 185 ms，明显滞后于 IN/OUT 表）。
延迟来源（按贡献排序）：
  1. 分析器内部缓冲积压：窗口取的是缓冲里“最旧”的一段，所以每多缓冲一个采样
     就多一份延迟。旧实现把上限设成 fftSize+8192（约 170 ms 音频），而且
     “生产=消费”意味着一旦积上去就永远待在那里 → 实测积压 9300 采样
     （≈194 ms），这是最主要的原因。
  2. 逐帧时间平滑 alpha=0.5（≈33 ms）。
  3. FFT 窗长 2048（42.7 ms）本身 + UI 拾取（≈10 ms）。
修复：
  · 缓冲上限改为 fftSize+4096（足够容纳宿主的整个音频块，块不被截断，
     所以 60 fps 不受块大小影响；FL Studio 常见 1024-4096 采样/块）。
  · 引入目标积压 fftSize+hop 与比例抽干：advance = hop + surplus/4，
     让积压收敛到目标而不是停在上限（宿主大块带来的富余是“新到的音频”，
     会被分摊到随后几帧，不会被抽干，所以帧率不掉；停滞的富余才是纯延迟）。
  · 逐帧平滑 alpha 0.5 → 0.35（对数频带平均已负责去噪，这里只需轻平滑）。
  · 编辑器打开时重置环形缓冲：此前无人观看期间积攒的整秒旧音频一次性灌入
     分析器会造成永久性积压（生产=消费，永远排不出去）。
实测（扫频+200Hz/6kHz 每 300ms 切换的信号，记录源切换时刻与显示曲线跨过
中点的时间；宿主块 480/1024/2048/4096 四种都测）：
  · 反应延迟 185.4 ms → 73.5~75.8 ms（约 2.5 倍），且与宿主块大小无关
  · 发布帧率 46.8~46.9 fps（去掉测试用的逐帧文件日志后约 52 fps）
  · 剩余延迟的大头是 2048 点 FFT 窗本身（窗中心 ≈21 ms）+ 平滑 + UI 拾取，
    已接近该窗长的物理下限；若想更快可把窗长降到 1024（频率分辨率减半）。

------------------------------------------------
【三、源码结构】
  src\PluginProcessor.h/.cpp   —— DSP 主处理（EQ + 染色模块 + 时延 + 频谱数据）
  src\PluginEditor.h/.cpp      —— 顶层编辑器
  src\Parameters.h/.cpp        —— 参数 ID / 枚举 / 参数布局工厂
  src\dsp\
      BiquadFilter.h           —— RBJ 双二阶（双精度）
      EQBand.h/.cpp            —— 单频段（级联节 + 参数平滑）
      EqualizerDSP.h/.cpp      —— 频段管理 + 相位模式 + 线性相位
      LinearPhaseFilter.h/.cpp —— FIR 设计 + overlap-add 卷积
      ColorProcessor.h         —— 染色模块（Warm/Cold/Clip）
      SpectrumAnalyzer.h/.cpp  —— FFT 频谱分析
  src\ui\
      LookAndFeel.h            —— CrunchPalette + CrunchLookAndFeel（与压缩机一致）
      DisplayView.h/.cpp       —— 频谱/曲线/频点交互/电平表
      BandControlPanel.h/.cpp  —— 底部功能区（COLOUR + 频段 + 全局 + In/Out）
      Fonts\                   —— 内嵌 Poppins 字体（构建依赖）
